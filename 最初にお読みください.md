# セットアップ手順（ブラウザだけで完了します）

リポジトリ: https://github.com/mysubsidypartner/Reporting-Support

所要時間 約3分。コマンド操作もGitのインストールも不要です。

---

## 手順1　ファイルをアップロードする

1. https://github.com/mysubsidypartner/Reporting-Support を開く
2. **Add file** → **Upload files** をクリック
3. このフォルダの**中身**（`build.py` `content` `.github` `README.md` `.gitignore`）を
   まとめてドラッグ＆ドロップ
4. 下の緑のボタン **Commit changes**

### 重要：`.github` フォルダが見えない場合

`.github` は隠しフォルダのため、初期状態では表示されません。
これが無いと自動デプロイが動きません。

- **Mac**: Finderで `command` + `shift` + `.`（ピリオド）を押す
- **Windows**: エクスプローラー上部の「表示」→「隠しファイル」にチェック

押すと半透明のフォルダが現れます。それも一緒にドラッグしてください。

> 隠しフォルダがどうしても扱いにくい場合は、
> このファイルの一番下「手順1の代替案」をご覧ください。

---

## 手順2　自動デプロイを有効にする

1. リポジトリ上部の **Settings**
2. 左メニューの **Pages**
3. Source のプルダウンを **GitHub Actions** に変更

保存ボタンはありません。選択した時点で反映されます。

---

## 手順3　確認する

1. 上部の **Actions** タブを開く
2. 黄色い丸が回っていればビルド中。1〜2分で緑のチェックに変わります
3. 完了後、Settings → Pages に表示されるURLでサイトが開きます

公開URL: `https://mysubsidypartner.github.io/Reporting-Support/`

---

## これ以降の更新（完全自動）

1. リポジトリで `content` フォルダを開く
2. 編集したいファイルをクリック
3. 右上の鉛筆アイコンで書き換える
4. **Commit changes**

これだけでビルドとデプロイが自動実行されます。手作業は一切ありません。

---

## 手順1の代替案（隠しフォルダを扱わない方法）

`.github` を除いた他のファイルだけ先にアップロードし、
ワークフローはGitHubの画面上で作成します。

1. `build.py` `content` `README.md` だけをアップロードして Commit
2. 上部の **Actions** タブを開く
3. **set up a workflow yourself** をクリック
4. 表示されたエディタの中身を全部消し、
   `.github/workflows/deploy.yml` の中身を貼り付ける
   （このフォルダ内にあります。テキストエディタで開けます）
5. 右上の **Commit changes**

`.github/workflows/` のフォルダはGitHubが自動で作ってくれるので、
隠しフォルダを操作する必要がありません。

その後、手順2に進んでください。
